import { css } from 'styled-components';


const variables = css `
:root {
    --easing: cubic-bezier(0.645, 0.045, 0.355, 1);
    --font-sans: 'Inter', sans-serif, monospace;
    --font-nunito: 'Nunito', sans-serif, monospace;
`


export default variables;